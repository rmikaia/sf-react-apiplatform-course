export const getGenericError = () => "Une erreur est survenue";

export const getCreateSuccess = (subject: string) =>
  `${subject} a bien été crée(e)`;

export const getEditSuccess = (subject: string) =>
  `${subject} a bien été modifié(e)`;

export const getDeleteSuccess = (subject: string) =>
  `${subject} a bien été supprimé(e)`;
